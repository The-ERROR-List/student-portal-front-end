import { Button } from 'react-bootstrap';
const ClassTool = () => {
    return (
        <div>
            <h1>ClassTool</h1>

            <Button variant="primary" size="lg">
                Create Zoom Meeting
            </Button>
        </div>
    )
}
export default ClassTool;